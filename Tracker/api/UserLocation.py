#Need API connection to get coordinates

class UserLocation:
    inputLocation = input(print("Where would you like to go?")) + "BC, Canada" #Need to change the format depending on the API

    #Get coordinates
    coordinates = 0 #Might be a list
    def coords():
        
        return